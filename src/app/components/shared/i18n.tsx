import React, { createContext, useContext, useState, useCallback } from "react";import bn from "../../i18n/bn.json";import en from "../../i18n/en.json";export type Locale = "bn" | "en";export type LocaleData = typeof bn;const locales: Record<Locale, LocaleData> = { bn, en };const LocaleCtx = createContext<{ locale: Locale; t: LocaleData; setLocale: (l: Locale) => void }>({ locale: "bn", t: bn, setLocale: () => {} });export function LocaleProvider({ children }: { children: React.ReactNode }) { const [locale, setLocale] = useState<Locale>("bn"); const toggle = useCallback((l: Locale) => { setLocale(l); try { document.cookie = "lang=" + l + ";path=/;max-age=31536000"; } catch {} }, []); return React.createElement(LocaleCtx.Provider, { value: { locale, t: locales[locale], setLocale: toggle } }, children); }export function useLocale() { return useContext(LocaleCtx); }export function t(path: string, locale: Locale) { const keys = path.split("."); let val: any = locales[locale]; for (const k of keys) { if (val == null) return path; val = val[k]; } return val ?? path; }